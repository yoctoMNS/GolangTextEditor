# CLAUDE.md — GolangTextEditor プロジェクトルール

このファイルは、このリポジトリで作業する全ての Claude セッション（および人間の開発者）が
守るべきプロジェクト全体のルールを定義する。作業前に必ず読むこと。

具体的な「絶対に守らなければならないチェックリスト」は
`.claude/skills/golang-editor-rules/SKILL.md` に分離してある。矛盾がある場合は
SKILL.md 側の禁止事項（MUST NOT）が優先する。

## 1. プロジェクトのゴール

Go言語で2Dゲームエンジンをプログラムベースで実装できるようにするための、
**テキストエディタとIDEの中間**に位置するソフトウェアを作る。

- **軽量・高速最優先**。機能の豊富さよりも起動速度・応答速度・メモリフットプリントを優先する。
- 対応OSは **Windows / Linux**。macOSは対象外（対応してはいけないという意味ではないが、
  優先度なし・検証もしない）。
- 想定描画/入力ライブラリ: `ebiten`, `go-sdl2`, `raylib-go` (raylib), `go-gl` (OpenGL)。
  実装言語はGoのみ。CGO前提のライブラリを使う場合でも、コア機能はCGOに依存させない
  （4章参照）。
- フェーズ1（現在）のゴールは **基本的なテキストエディタ機能** の実装。
  ゲームエンジン統合・スクリプト実行・シンタックスハイライト等は将来フェーズ。
  現時点でそれらを先取りして実装しない（過剰設計の禁止、SKILL.md 参照）。

## 2. アーキテクチャ方針

レイヤーを明確に分離し、依存方向は必ず「外側 → 内側」の一方向にする。

```
cmd/editor/          … エントリポイント (main関数、CLI引数、ウィンドウ生成)
internal/app/        … 描画・入力バックエンド実装 (現状: ebiten)
internal/editor/      … 編集コマンド・カーソル・ファイルI/O (バックエンド非依存)
internal/buffer/      … テキスト本体のデータ構造 (バックエンド非依存)
```

- `internal/buffer` と `internal/editor` は **いかなる描画/ウィンドウ/入力ライブラリも
  importしてはならない**。純粋なGo標準ライブラリのみに依存する。
  これにより、ディスプレイの無い環境（CI・コンテナ）でも `go test` で全ロジックを検証できる。
- 別の描画バックエンド（raylib-go, go-sdl2, go-gl）に切り替える／追加する場合は、
  `internal/app` 相当の新パッケージ（例: `internal/app/raylib`）を追加し、
  `internal/editor.Editor` を呼び出すだけにする。`internal/editor` や
  `internal/buffer` を書き換える必要が生じた場合は設計ミスを疑うこと。
- `internal/app` (現行の ebiten 実装) は「入力イベント → editorのメソッド呼び出し」
  「editorの状態 → 画面描画」という薄い変換層に留める。編集ロジック（カーソル移動の
  境界処理、行結合など）をこの層に書かない。

## 3. テキストバッファの実装方針

- 現状は「行ごとに `[]rune` を保持する」シンプルな実装 (`internal/buffer`) を採用している。
  実装が単純で、正しさの検証が容易なため。
- 大規模ファイル・高頻度編集でプロファイリング上のボトルネックが実測された場合のみ、
  ギャップバッファ／ロープ／ピーステーブルへの置き換えを検討する。
  **測定なしに複雑なデータ構造へ先回りして書き換えない。**
- 1回のキー入力（1文字挿入・1文字削除）に対する計算量は、その行の長さに比例する
  範囲に収める。バッファ全体をコピーするような実装をしない。

## 4. ライブラリ選定ルール

- フェーズ1のGUI実装は `github.com/hajimehoshi/ebiten/v2` を採用する。理由:
  - Windows/Linux 双方でクロスコンパイルが容易（Windows向けは `CGO_ENABLED=0` で
    ビルド可能）。
  - Linuxでも `purego`/`glfw` 経由でX11と連携し、フォントはビットマップフォント
    (`golang.org/x/image/font/basicfont`) を使うことでTTFパースや外部フォント
    ファイルへの依存を避けている＝軽量。
- `go-sdl2`, `raylib-go`, `go-gl` への切替・追加は将来のフェーズで検討する。
  採用する場合も、上記アーキテクチャ方針（2章）に従い `internal/app` 配下に
  隔離すること。
- 新しい外部依存を追加する前に、本当に必要か（標準ライブラリで足りないか）を検討する。
  「軽量・高速」の目標に反する重量級ライブラリ（フルGUIツールキット、大きな
  シンタックスハイライトエンジン等）は慎重に検討し、導入前に一言相談する。

## 5. コーディング規約

- `gofmt` 済みであること。インデントやスタイルの手直しのためだけの巨大diffを作らない。
- `go vet ./...` が警告なしで通ること。
- パッケージ・関数コメントは Go の doc comment 規約に従う（何をするかではなく、
  非自明な制約・理由を書く。自明なコメントは書かない）。
- グローバルな可変状態を持たない。状態は `editor.Editor` / `buffer.Buffer` の
  インスタンスに閉じ込める。
- エラーは握りつぶさず呼び出し元に伝播させる。UI層（`internal/app`）で
  ユーザー向けメッセージに変換する。

## 6. テスト方針

- `internal/buffer`, `internal/editor` の全ての公開関数は、ディスプレイ無しで
  実行できる `go test` でカバーする。新機能追加時は対応するテストを同じPR内で追加する。
- `internal/app` (ebiten依存層) は自動テストの対象外で構わないが、変更した場合は
  実機・実環境（実際にウィンドウを開ける環境）で動作確認したことをコミットメッセージや
  PR説明に明記する。ヘッドレス環境でのビルド確認（`go build ./...`、Xvfb上での
  起動確認）は最低限行う。
- コミット前に最低限 `gofmt -l .`, `go vet ./...`, `go test ./...` を実行する。
  詳細な必須項目は SKILL.md を参照。

## 7. パフォーマンス方針

- 目標フレームタイムは 16ms（60fps）以下。`Update`/`Draw` 内で毎フレーム
  ヒープアロケーションが発生する処理（特に文字列結合、スライスの再確保）は避ける。
- キー入力からカーソル移動・文字挿入までの体感遅延をゼロに近づけることを優先する
  （余計な抽象化・非同期処理を挟まない）。

## 8. 過剰設計の禁止

- 今のフェーズで要求されていない機能（マルチタブ、プラグインシステム、
  ゲームエンジンのランタイム統合など）を先回りして実装しない。
- 3行程度の類似コードのために早すぎる抽象化をしない。
- 使われていない設定・フラグ・インターフェースを追加しない。
